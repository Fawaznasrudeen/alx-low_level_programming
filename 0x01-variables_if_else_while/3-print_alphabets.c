ii#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		puchar(ch);
	for (ch = 'A'; ch <= Z'; ch++)
		puchar(ch);
	putchar('\n');
	return (0);
}
